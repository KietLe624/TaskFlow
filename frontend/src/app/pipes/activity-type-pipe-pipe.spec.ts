import { ActivityTypePipePipe } from './activity-type-pipe-pipe';

describe('ActivityTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivityTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
