import { ProjectPriorityPipe } from './project-priority-pipe';

describe('ProjectPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
